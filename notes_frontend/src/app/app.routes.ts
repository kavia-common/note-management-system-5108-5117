import { Routes } from '@angular/router';

/**
 * PUBLIC_INTERFACE
 * App Routes
 * Currently the app is a single-screen layout orchestrated by AppComponent.
 * This routes array is kept for future extensibility (e.g., /note/:id deep links).
 */
export const routes: Routes = [];
